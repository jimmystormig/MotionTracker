# Seal this with: kubeseal --format yaml < this-file.yaml > motiontracker-db-credentials.sealed.yaml
apiVersion: v1
kind: Secret
metadata:
  name: motiontracker-db-credentials
  labels:
    app.kubernetes.io/part-of: motiontracker
type: kubernetes.io/basic-auth
stringData:
  username: motiontracker
  password: CHANGE_ME_SECURE_PASSWORD
